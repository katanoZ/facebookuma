<div class="container">
  <h2>ユーザ一覧</h2>
  <% @users.each do |user| %>
    <% next if user.id == current_user.id %>
    <%= render "person", user:user %>
  <%end%>
  <% myself = @users.find(current_user.id) %>
  <%= render "person", user:myself %>
</div>
